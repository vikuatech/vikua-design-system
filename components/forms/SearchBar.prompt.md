Barra de busqueda global de la plataforma. Es el unico lugar del sistema con rotulo en mayusculas.

```jsx
<SearchBar placeholder="Buscar" onSearch={run} onFilters={openFilters} />
<SearchBar compact onSearch={run} />
```
